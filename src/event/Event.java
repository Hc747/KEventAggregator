package event;

/**
 * An event is a self-contained concrete class with no behavioural implementations used to pass
 * state between event handlers. An event's state should be immutable.
 */
public abstract class Event {}
